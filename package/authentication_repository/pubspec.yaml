name: authentication_repository
description: A package for authentication
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.4.0
  firebase_auth: ^3.1.1
  google_sign_in: ^5.1.0
  equatable: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

